<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="rule_multi_company_hr_attendance_reason" model="ir.rule">
        <field name="name">Attendance Reason multi-company</field>
        <field name="model_id" ref="model_hr_attendance_reason" />
        <field name="global" eval="True" />
        <field
            name="domain_force"
        >['|',('company_id','=',False),('company_id','in',company_ids)]</field>
    </record>

    
    <record id="rule_hr_attendance_user_own" model="ir.rule">
        <field name="name">Attendance: Own Records</field>
        <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
        <field name="groups" eval="[(4, ref('hr_attendance_reason.group_hr_attendance_user'))]"/>
        <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
    </record>

</odoo>
